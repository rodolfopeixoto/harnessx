# $NAME

Scaffolded by `harness scaffold python`.

## Quick start

```bash
make setup        # creates .venv + installs deps
make lint         # ruff
make test         # pytest
make run          # uvicorn dev server
curl localhost:8000/healthz
```
