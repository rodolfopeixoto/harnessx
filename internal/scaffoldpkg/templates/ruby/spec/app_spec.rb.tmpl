require 'spec_helper'
require 'json'

RSpec.describe 'GET /healthz' do
  it 'returns ok status' do
    get '/healthz'
    expect(last_response.status).to eq(200)
    body = JSON.parse(last_response.body)
    expect(body['status']).to eq('ok')
    expect(body).to have_key('env')
  end
end
