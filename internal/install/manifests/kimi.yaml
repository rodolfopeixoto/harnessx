name: kimi
description: Moonshot Kimi CLI (kimi-cli v1.46+)
category: agent
probe:
  binary: kimi-cli
  version_args: ["--version"]
strategies:
  - kind: pip_user
    args: { package: "kimi-cli" }
  - kind: brew
    platform: { os: [darwin] }
    args: { package: "kimi-cli" }
