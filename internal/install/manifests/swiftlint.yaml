name: swiftlint
description: Swift linter from Realm
category: lint
probe:
  binary: swiftlint
  version_args: ["--version"]
strategies:
  - kind: brew
    platform: { os: [darwin] }
    args: { package: swiftlint }
