name: codex
description: OpenAI Codex CLI
category: agent
probe:
  binary: codex
  version_args: ["--version"]
strategies:
  - kind: brew
    platform: { os: [darwin] }
    args: { package: codex }
  - kind: npm_global
    args: { package: "@openai/codex" }
