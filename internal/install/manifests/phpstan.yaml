name: phpstan
description: PHP static analysis
category: typecheck
probe:
  binary: phpstan
  version_args: ["--version"]
strategies:
  - kind: brew
    platform: { os: [darwin] }
    args: { package: phpstan }
  - kind: composer_global
    args: { package: phpstan/phpstan }
